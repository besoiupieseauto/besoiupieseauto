# Baza de date besoiupieseauto.ro

- Schema in git: `SQL/besoiupieseauto_schema.sql`
- Dump complet (~11 GB): nu poate fi urcat pe GitHub (limita 100 MB/fisier).
  Foloseste backup local Laragon: `D:\laragon\backup\mysql\` sau dump manual.

Restore schema:
```
mysql -uroot < SQL/besoiupieseauto_schema.sql
```
